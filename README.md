# QR Split

    deno --allow-write=output run main.ts 3 "Some string to encode"

Generates `.svg` files in `output/`. They can be printed on transparencies, laid
on top of each other, and then the full QR code will appear.
